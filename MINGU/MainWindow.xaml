<Window x:Class="MinguApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MINGU - Auto Typer" Height="650" Width="750"
        Background="#F5F5F5"
        Loaded="Window_Loaded"
        Closed="Window_Closed">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header with Always on Top -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="MINGU - Auto Typer"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#333"
                       VerticalAlignment="Center" />

            <CheckBox x:Name="AlwaysOnTopCheckbox"
                      Grid.Column="1"
                      Content="Always on Top"
                      FontSize="12"
                      VerticalAlignment="Center"
                      Checked="AlwaysOnTopCheckbox_Changed"
                      Unchecked="AlwaysOnTopCheckbox_Changed" />
        </Grid>

        <!-- Favorites Section -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#DDD"
                BorderThickness="1"
                CornerRadius="5"
                Padding="10"
                Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="FavoritesComboBox"
                          Grid.Column="0"
                          FontSize="13"
                          Height="30"
                          SelectionChanged="FavoritesComboBox_SelectionChanged"
                          DisplayMemberPath="Name" />

                <Button x:Name="SaveFavoriteButton"
                        Grid.Column="1"
                        Content="💾 Save"
                        Height="30"
                        Width="70"
                        Margin="5,0,0,0"
                        FontSize="12"
                        Click="SaveFavoriteButton_Click"
                        Background="#2196F3"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="3"
                                                Padding="5">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="DeleteFavoriteButton"
                        Grid.Column="2"
                        Content="🗑️ Delete"
                        Height="30"
                        Width="70"
                        Margin="5,0,0,0"
                        FontSize="12"
                        Click="DeleteFavoriteButton_Click"
                        Background="#F44336"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="3"
                                                Padding="5">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- Text Input Area -->
        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#DDD"
                BorderThickness="1"
                CornerRadius="5"
                Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Enter or paste your text below:"
                           FontSize="12"
                           Foreground="#666"
                           Margin="0,0,0,5" />

                <TextBox x:Name="InputBox"
                         Grid.Row="1"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"
                         FontSize="14"
                         FontFamily="Segoe UI"
                         BorderThickness="0"
                         Background="Transparent"
                         Padding="5"
                         TextChanged="InputBox_TextChanged" />

                <!-- Character Counter -->
                <TextBlock x:Name="CharacterCounter"
                           Grid.Row="2"
                           Text="0 characters"
                           FontSize="11"
                           Foreground="#999"
                           HorizontalAlignment="Right"
                           Margin="0,5,0,0" />
            </Grid>
        </Border>

        <!-- Typing Speed Control -->
        <Border Grid.Row="3"
                Background="White"
                BorderBrush="#DDD"
                BorderThickness="1"
                CornerRadius="5"
                Padding="12"
                Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Typing Speed:"
                           FontSize="13"
                           FontWeight="SemiBold"
                           Foreground="#333"
                           VerticalAlignment="Center"
                           Margin="0,0,15,0" />

                <Slider x:Name="SpeedSlider"
                        Grid.Column="1"
                        Minimum="1"
                        Maximum="3"
                        Value="2"
                        TickFrequency="1"
                        TickPlacement="BottomRight"
                        IsSnapToTickEnabled="True"
                        VerticalAlignment="Center"
                        ValueChanged="SpeedSlider_ValueChanged" />

                <TextBlock x:Name="SpeedLabel"
                           Grid.Column="2"
                           Text="Medium"
                           FontSize="13"
                           FontWeight="Bold"
                           Foreground="#4CAF50"
                           VerticalAlignment="Center"
                           Margin="15,0,0,0"
                           MinWidth="70"
                           TextAlignment="Center" />
            </Grid>
        </Border>

        <!-- Buttons Row -->
        <Grid Grid.Row="4" Margin="0,15,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Start Button -->
            <Button x:Name="StartTypingButton"
                    Grid.Column="0"
                    Height="45"
                    Content="Start typing (Ctrl+Alt+M)"
                    Click="StartTypingButton_Click"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Background="#4CAF50"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Margin="0,0,10,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#4CAF50" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="5"
                                            Padding="10">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCC" />
                                <Setter Property="Foreground" Value="#666" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- Clear Button -->
            <Button x:Name="ClearButton"
                    Grid.Column="1"
                    Height="45"
                    Width="100"
                    Content="Clear"
                    Click="ClearButton_Click"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Background="#FF5722"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#FF5722" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="5"
                                            Padding="10">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E64A19" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <!-- Status Text -->
        <Border Grid.Row="5"
                Background="#E3F2FD"
                BorderBrush="#2196F3"
                BorderThickness="1"
                CornerRadius="5"
                Padding="10">
            <TextBlock x:Name="StatusText"
                       FontSize="13"
                       Foreground="#1976D2"
                       Text="Ready. Hotkey: Ctrl+Alt+M | Enter text and click start or use hotkey."
                       TextWrapping="Wrap" />
        </Border>

        <!-- Countdown Overlay -->
        <Border x:Name="CountdownOverlay"
                Grid.Row="0"
                Grid.RowSpan="7"
                Background="#CC000000"
                Visibility="Collapsed"
                CornerRadius="10">
            <TextBlock x:Name="CountdownText"
                       Text="3"
                       FontSize="120"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center" />
        </Border>
    </Grid>
</Window>
